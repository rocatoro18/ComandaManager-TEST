<?php

namespace Tests\Feature;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Foundation\Testing\WithFaker;
use Tests\TestCase;
use App\Models\User;


//class AplicacionTest extends TestCase
//{
    /** @test */
    //use RefreshDatabase;

    /*
    public function verificar_que_se_crea_usario_al_iniciar_aplicacion()
    {

    $this->withoutExceptionHandling();

    // Creamos observador
    $mock = $this->createMock(User::class);
    $mock->expects($this->once())->method('create')->with('Usuario creado');

    // Verificamos que al correr la aplicación se crea el usuario
    $app = new Aplication();

    $app -> run($mock);

    }
    */


//}
